spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Medicine
    username: root
    password: al-malikmysql123
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
#  thymeleaf:  # Fixed indentation for Thymeleaf configuration
#    prefix: classpath:/templates/  # Removed "PurchaseList"
#    suffix: .html  # Removed "PurchaseList.html"